# customer/project sample documents: never commit these
samples/

# build output
target/
# every build tree, however it is named: `shell/build-release` was committed
# whole once — 781 files — because only `shell/build/` was listed here.
shell/build*/
build/

# editor / tooling
.cache/
compile_commands.json
*.swp

# packaged output
dist/

# the portable Qt and its sources, built by packaging/appimage/build-qt.sh
packaging/appimage/toolchain/
